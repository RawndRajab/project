import 'package:flutter/material.dart';

void main() {
  runApp(
    MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: const Text('hi'),
        ),
        body: Center(child: const Text('welcome')),
      ),
    ),
  );
}
